node_modules/
dist/
/.seai-data/
# Runtime Obsidian vault data (repo ROOT only). NOTE: src/brain/ is application
# source code and MUST stay tracked — never ignore it.
# (A bare `brain/` pattern would wrongly match src/brain/ at every level.)
/brain/
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db
coverage/
.shopify/
*.sqlite

.vercel
.env*
